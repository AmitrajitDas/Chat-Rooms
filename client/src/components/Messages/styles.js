import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
 
    messages: {
        padding: '5% 0',
        overflow: 'auto',
        flex: 'auto',
    },

}));